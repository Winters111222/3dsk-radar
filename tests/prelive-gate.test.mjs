import test from "node:test";import assert from "node:assert/strict";import { readFile } from "node:fs/promises";const search=await readFile(new URL("../netlify/functions/search.mjs",import.meta.url),"utf8");const env=await readFile(new URL("../.env.example",import.meta.url),"utf8");test("paid search is guarded by explicit final-acceptance kill switch",()=>{assert.match(search,/LIVE_AI_LOCKED/);assert.ok(search.indexOf("LIVE_AI_LOCKED")<search.indexOf("OPENAI_API_KEY"));assert.match(env,/^RADAR_LIVE_AI_ENABLED=false$/m);});
