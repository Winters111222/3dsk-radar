[build]
  command = "npm run build"
  publish = "."
  functions = "netlify/functions"

[functions]
included_files = ["build-metadata.json", "config/company-profile.public.json", "config/source-historical-qualification.v1.json", "fixtures/opportunities.json"]

[dev]
  publish = "."
